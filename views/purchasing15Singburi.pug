include css.pug
doctype html
html
  head
    meta(charset="utf-8")
    title report
  body
    button.btn.btn-sm.btn-success.btnprint(onclick='window.print()') Print
    .center.bold ประกาศ#{hospitalName}
    .center.bold เรื่อง ประกาศผู้ชนะการเสนอราคา 
    .center.bold 
      | ซื้อจำนวน #{countp} รายการ ( 
      - var y=0
            for b in purchasing
              - y++
              | &nbsp;#{b.generic_name}
              if(y<countp)
                | , 
      | ) โดย#{bidname}
    br
    .center
      | ---------------------------------------------------------
    br
    .fontspace
    | ตามที่#{hospitalName} ได้มีโครงการ ซื้อจำนวน #{countp} รายการ ( 
    - var y=0
          for b in purchasing
            - y++
            | &nbsp;#{b.generic_name}
            if(y<countp)
              | , 
    | ) โดย#{bidname} นั้น
    br
    .fontspace
    - var y=0
          for b in purchasing
            - y++
             | #{b.generic_name}&nbsp; จำนวน #{b.qtyPoi} x #{b.conversion} #{b.primary_unit} ผู้ได้รับคัดเลือกได้แก่ บริษัท #{lname} (ส่งออก,ขายส่ง,ขายปลีก,ให้บริการ,ผู้ผลิต) โดยเสนอราคา เป็นเงินทั้งสิ้น #{b.total_price} บาท (#{bahtText}) รวมภาษีมูลค่าเพิ่มและภาษีอื่น ค่าขนส่ง ค่าจดทะเบียน และค่าใช้จ่ายอื่นๆทั้งปวง
            br
            .fontspace
    br
    br
    .right ประกาศ ณ วันที่ #{nDate}
    br
    br
    div(style="margin-left: 40%;width:80%;float:left;text-align:center")
      br
      br
      br
      | ลงชื่อ............................................................
      br
      | ( #{poraor} )
      br
      | ผู้อำนวยการ#{hospitalName}